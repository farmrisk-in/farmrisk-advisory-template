This repo contains the export template for advisory as a npm package.

This package is must be installed in both webapp and anddroid app (react native)

then changing anything here will reflect in both apps.
